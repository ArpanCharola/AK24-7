# Python
backend/venv/
backend/__pycache__/
backend/**/__pycache__/
backend/*.egg-info/
backend/.env
backend/celerybeat-schedule
backend/celerybeat-schedule.*
# Bytecode + tool caches (anywhere in the tree)
*.pyc
*.pyo
*.pyd
.pytest_cache/
.ruff_cache/
.mypy_cache/
# Local backend logs (uvicorn/celery output redirected to files)
backend/*.log

# User-uploaded content (PII — résumés, cover letters, tailored output)
backend/uploads/

# Node
frontend/node_modules/
frontend/dist/
frontend/.env

# Nested shadcn admin dashboard reference template — not part of this project
/ai apply/

# Claude Code local state (not part of the application)
.claude/
CLAUDE.md

# Deployment notes — local only
DEPLOY.md

# Internal planning / status notes (Others/) and the job-sourcing research
# knowledge base (sources/) — useful working docs, but not part of the product repo
Others/
sources/

# Playwright MCP session captures (console logs, screenshots) — local artifacts
.playwright-mcp/

# Local agent memory / handoff system (not part of the app)
.remember/

# OS
.DS_Store
Thumbs.db

# SDD workflow scratch (local only)
.superpowers/
docs